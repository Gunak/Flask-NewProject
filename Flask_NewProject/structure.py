from CreateDirs import create_dirs


class Structure:
    """Creation of Flask directories."""

    def __init__(self, projectName):
        self.projectName = projectName

    def simple_setup(self, preload=False):
        pass


    def simple_package(self, preload=False):
        pass

    def blueprint_package(self, sytle="divisional", preload=False):
        if style.lower() == "functional":
            pass
        
        elif style.lower() == "divisional":
            pass
        
        else:
            print("Not a known style.")

    
